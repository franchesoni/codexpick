# Optional: directory that contains auth.json and auth-*.json snapshots.
# Defaults to ~/.codex when unset.
CODEXPICK_HOME=~/.codex

# Optional: exact Codex CLI binary to probe/launch.
# Defaults to the first `codex` found on PATH.
CODEX_BIN=/path/to/codex
